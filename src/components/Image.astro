---
// @ts-ignore
import { Img } from "astro-imagetools/components";

export interface Props extends astroHTML.JSX.ImgHTMLAttributes {
  src: string
  alt: string
}

---

<Img loading="lazy" breakpoints={{ count: 4, minWidth: 300, maxWidth: 1024 }} {...Astro.props} background="transparent" />